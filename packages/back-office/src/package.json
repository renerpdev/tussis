{
  "name": "back-office",
  "description": "Firebase back-office package",
  "scripts": {
    "lint": "nx lint back-office",
    "build": "nx build back-office",
    "serve": "npm run build && firebase emulators:start --only functions --config firebase.json",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions --config firebase.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "16"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
